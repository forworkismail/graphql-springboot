scalar Date

type Query {
    pets: [Pet]
    owners: [Person]
    favoritePet(ownerId: String!): Pet
    petsByOwner(ownerId: String!): [Pet]
    pet(id: ID!): Pet
}

type Pet {
    id: ID!
    name: String
    owner: Person
    dateOfBirth: Date
}

type Person {
    firstName: String
    lastName: String
}