type Query {
    pets: [Pet]
    owners: [Person]
    favoritePet(ownerId: String!): Pet
    petsByOwner(ownerId: String!): [Pet]
}

type Pet {
    name: String
    owner: Person
}

type Person {
    firstName: String
    lastName: String
}